import type { NextConfig } from "next";

const nextConfig: NextConfig = {
  /* config options here */
  images: {
    domains: ["momoslimes.com", "cdn.mall.adeptmind.ai"], // ✅ Add the domain here
  },
};

export default nextConfig;
